# P2-007 - Validate EvidenceCard-only Bullet ID Namespace

Status: DONE

## Task ID

P2-007

## Title

Validate EvidenceCard-only Bullet ID Namespace

## Objective

Reject Writer outputs whose visible work bullets use non-EvidenceCard IDs in `bullet.evidenceIds`.

## Repository Evidence

- `docs/governance/phase2/QUALITY_GAP_ANALYSIS.md`: AI Evaluation CV contains 11 non-EvidenceCard IDs in bullet evidence arrays.
- `docs/governance/phase2/ROOT_CAUSE_ANALYSIS.md` RC-05: Writer output contract is structurally valid but quality-weak.
- `docs/governance/phase2/IMPLEMENTATION_PRIORITY_MATRIX.md` rank 5: quality-invalid output can be applied.
- `docs/governance/phase2/QUALITY_SCORECARD.md`: Evidence Traceability is a Critical metric.
- `CV_Manager_React/src/domain/screeningCvOutput.ts`: current Writer-output validation owner.
- `CV_Manager_React/scripts/smoke-writer-output.mjs`: existing Writer-output smoke coverage.

Confidence: Confirmed

## Root Cause Addressed

RC-05: Writer Output contract does not enforce the `QUALITY_SPEC.md` dimensions required before apply.

## Scope

Single responsibility: validate the namespace of `TailoredCv` bullet evidence IDs before apply.

In scope:

- EvidenceCard ID namespace validation for work-experience bullet evidence IDs.
- Exact validation errors identifying invalid paths.
- Focused smoke fixture proving mixed ID namespaces fail.

Out of scope:

- Trace coverage thresholds.
- Duplicate content validation.
- Contact completeness.
- Reviewer semantics.
- Prompt wording.
- Runtime data migration.

## Files Allowed to Change

- `CV_Manager_React/src/domain/screeningCvOutput.ts`
- `CV_Manager_React/scripts/smoke-writer-output.mjs`
- `CV_Manager_React/package.json` only if a new focused script is necessary
- `docs/governance/tasks/P2-007.md`
- `docs/governance/phase2/Wave_Completion_Report.md` only after the full Wave 2 implementation is authorized and completed

## Forbidden Files

- `CV_Manager_React/src/promptBuilders.ts`
- `CV_Manager_React/src/data/selection.ts`
- `CV_Manager_React/src/domain/screeningReview.ts`
- `CV_Manager_React/src/domain/localReviewerFix.ts`
- `CV_Manager_React/data/**`
- `CV_Manager_React/data/prompt_templates.json`
- `docs/governance/contracts/**`
- `docs/governance/DECISIONS.md`
- Any runtime prompt or production behavior outside Writer-output validation

## Dependencies

- P2-003 DONE: evidence priority preservation from Wave 1.
- Wave 1 completion accepted: `docs/governance/phase2/Wave1_Completion_Report.md`.

Dependency validation: Satisfied.

## Acceptance Criteria

- [x] A bullet evidence ID that does not match an existing EvidenceCard ID fails validation.
- [x] Validation error reports the exact bullet path.
- [x] A bullet containing mixed valid and invalid IDs fails validation.
- [x] Existing valid Writer-output fixture still passes.
- [x] No canonical runtime data is modified.

## Required Tests

Run after task completion:

- `npm run smoke:writer-output`

## Regression Risks

- Existing model outputs with skill/story/domain IDs may be rejected.
- Legacy persisted CVs must remain readable; this task validates new apply boundaries only.

## Rollback Strategy

Revert changes to `screeningCvOutput.ts` and the Writer-output smoke fixture.

## Definition of Done

P2-007 is done only when invalid bullet evidence namespaces fail deterministically and the focused Writer-output smoke test passes.

## Completion Report Format

```text
Task: P2-007
Status:
Files changed:
Validation added:
Fixtures:
Tests run:
Acceptance criteria:
Regression risks:
Follow-up:
```

## Completion Report

Task: P2-007  
Status: DONE  
Files changed:

- `CV_Manager_React/src/domain/screeningCvOutput.ts`
- `CV_Manager_React/scripts/smoke-writer-output.mjs`
- `docs/governance/tasks/P2-007.md`
- `docs/governance/MASTER_TASK_ROADMAP.md`
- `docs/governance/CURRENT_STATE.md`
- `docs/governance/COMPLETION_REPORT.md`

Validation added:

- `validateScreeningCvOutput` now rejects non-`evi-` bullet evidence ID namespaces.
- `validateScreeningCvOutput` accepts an optional `validEvidenceIds` context and rejects unknown EvidenceCard IDs when supplied.
- Error messages include exact paths such as `workExperience[0].subsections[0].bullets[0].evidenceIds[1]`.

Fixtures:

- Valid Tailored CV with valid EvidenceCard ID passes.
- Mixed namespace fixture with `skill-`, `star-`, and `domain-` IDs fails at exact paths.
- Unknown `evi-` ID fails when validation context is provided.

Tests run:

- `npm run smoke:writer-output` - PASS
- `npm run build` - PASS
- `npm run test:system` - PASS after approved localhost rerun for `smoke:server`

Acceptance criteria:

- [pass] A bullet evidence ID that does not match an existing EvidenceCard ID fails validation.
- [pass] Validation error reports the exact bullet path.
- [pass] A bullet containing mixed valid and invalid IDs fails validation.
- [pass] Existing valid Writer-output fixture still passes.
- [pass] No canonical runtime data is modified.

Regression risks:

- New Writer outputs with skill/story/domain IDs in bullet evidence arrays will now fail validation.
- Exact existing-ID validation requires callers to provide `validEvidenceIds`; existing one-argument call sites remain backward compatible.

Follow-up:

- `docs/governance/phase2/IMPLEMENTATION_PRIORITY_MATRIX.md` is referenced by this task but does not exist at that path in the repository. The task had enough supporting evidence from the other listed files, so implementation proceeded and this path issue is recorded here.
- P2-008 was not executed or promoted.
