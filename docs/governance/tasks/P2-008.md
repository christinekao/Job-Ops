# P2-008 - Validate Trace Coverage and Duplicate Visible Content Before Apply

Status: DONE

## Task ID

P2-008

## Title

Validate Trace Coverage and Duplicate Visible Content Before Apply

## Objective

Prevent Writer outputs with insufficient bullet trace coverage or duplicate visible content from being applied.

## Repository Evidence

- `docs/governance/phase2/QUALITY_GAP_ANALYSIS.md`: both inspected CVs have five bullets without IDs.
- `docs/governance/phase2/QUALITY_GAP_ANALYSIS.md`: Power Platform CV has duplicated summary/repair-note patterns.
- `docs/governance/phase2/ROOT_CAUSE_ANALYSIS.md` RC-05: structural validation does not enforce trace coverage or duplicate text.
- `docs/governance/phase2/QUALITY_SCORECARD.md`: Evidence Traceability and Duplicate Content are measured quality metrics.
- `CV_Manager_React/src/domain/screeningCvOutput.ts`: Writer-output validation owner.
- `CV_Manager_React/scripts/smoke-writer-output.mjs`: existing Writer-output smoke coverage.

Confidence: Confirmed

## Root Cause Addressed

RC-05: Writer-output validation is structurally valid but quality-weak.

## Scope

Single responsibility: add pre-apply validation for trace coverage and duplicate visible content.

In scope:

- Minimum trace coverage rule for major work bullets.
- Duplicate summary/bullet visible-text detection.
- Fixtures based on persisted bad-output shapes without mutating canonical data.

Out of scope:

- EvidenceCard namespace validation covered by P2-007.
- Contact completeness covered by P2-009.
- Reviewer semantics covered by P2-010 and P2-011.
- Prompt wording.

## Files Allowed to Change

- `CV_Manager_React/src/domain/screeningCvOutput.ts`
- `CV_Manager_React/scripts/smoke-writer-output.mjs`
- `CV_Manager_React/package.json` only if a new focused script is necessary
- `docs/governance/tasks/P2-008.md`
- `docs/governance/phase2/Wave_Completion_Report.md` only after the full Wave 2 implementation is authorized and completed

## Forbidden Files

- `CV_Manager_React/src/promptBuilders.ts`
- `CV_Manager_React/src/data/selection.ts`
- `CV_Manager_React/src/domain/screeningReview.ts`
- `CV_Manager_React/src/domain/localReviewerFix.ts`
- `CV_Manager_React/data/**`
- `CV_Manager_React/data/prompt_templates.json`
- `docs/governance/contracts/**`
- `docs/governance/DECISIONS.md`

## Dependencies

- P2-007 DONE.

Dependency validation: Satisfied. P2-007 is DONE and accepted for P2-008 readiness.

## Acceptance Criteria

- [x] Writer output with untraced major bullets fails before apply.
- [x] Writer output with duplicate visible summary or duplicate bullet text fails before apply.
- [x] Valid traceable output fixture passes.
- [x] Persisted bad-output shapes can be represented as fixtures without modifying runtime data.
- [x] No canonical runtime data is modified.

## Required Tests

Run after task completion:

- `npm run smoke:writer-output`

## Regression Risks

- Stricter validation may reject existing model responses that previously applied successfully.
- Trace thresholds must not reject allowed prior-role compression when evidence is intentionally omitted by contract.

## Rollback Strategy

Revert changes to `screeningCvOutput.ts` and related Writer-output fixtures.

## Definition of Done

P2-008 is done only when bad trace coverage and duplicate visible content fail deterministically while valid output still passes.

## Completion Report Format

```text
Task: P2-008
Status:
Files changed:
Validation added:
Fixtures:
Tests run:
Acceptance criteria:
Regression risks:
Follow-up:
```

## Completion Report

Task: P2-008  
Status: DONE  
Files changed:

- `CV_Manager_React/src/domain/screeningCvOutput.ts`
- `CV_Manager_React/scripts/smoke-writer-output.mjs`
- `docs/governance/tasks/P2-008.md`
- `docs/governance/MASTER_TASK_ROADMAP.md`
- `docs/governance/CURRENT_STATE.md`
- `docs/governance/COMPLETION_REPORT.md`

Validation added:

- Visible work bullets now require at least one EvidenceCard ID.
- Duplicate normalized summary sentences are rejected.
- Duplicate normalized visible work bullet text is rejected.

Fixtures:

- Valid traceable output passes.
- Untraced visible work bullet fails.
- Duplicate summary sentence fails.
- Duplicate visible work bullet text fails.

Tests run:

- `npm run smoke:writer-output` - PASS
- `npm run build` - PASS
- `npm run test:system` - PASS after approved localhost rerun for `smoke:server`

Acceptance criteria:

- [pass] Writer output with untraced major bullets fails before apply.
- [pass] Writer output with duplicate visible summary or duplicate bullet text fails before apply.
- [pass] Valid traceable output fixture passes.
- [pass] Persisted bad-output shapes can be represented as fixtures without modifying runtime data.
- [pass] No canonical runtime data is modified.

Regression risks:

- Writer outputs with intentionally untraced prior-role bullets will now fail validation unless a future task defines an allowed exception.
- Duplicate detection is normalized exact text matching; near-duplicate semantic repetition remains future quality-evaluation scope.

Follow-up:

- P2-009 was not executed or promoted.
